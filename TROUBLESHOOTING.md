# 故障排除指南

## 开发服务器启动问题

### 问题：`npm run dev` 卡在 "Starting..." 状态

#### 可能原因和解决方案

1. **复杂的 Webpack 配置**
   - **症状**: 开发服务器启动缓慢或卡住
   - **解决方案**: 简化 `next.config.ts` 中的 webpack 配置
   - **操作**: 移除复杂的 `splitChunks` 配置，只保留基本优化

2. **缓存问题**
   - **症状**: 配置更改后服务器无法启动
   - **解决方案**: 清理 Next.js 缓存
   ```bash
   rm -rf .next
   npm run dev
   ```

3. **端口冲突**
   - **症状**: 端口 3000 被占用
   - **解决方案**: 终止占用端口的进程或使用其他端口
   ```bash
   # 查找占用端口的进程
   lsof -ti:3000
   
   # 终止进程
   kill -9 $(lsof -ti:3000)
   
   # 或使用其他端口
   npm run dev -- -p 3001
   ```

4. **进程残留**
   - **症状**: 之前的开发服务器进程仍在运行
   - **解决方案**: 终止所有 Next.js 进程
   ```bash
   pkill -f "next dev"
   ```

### 快速修复步骤

1. **终止所有相关进程**
   ```bash
   pkill -f "next dev"
   ```

2. **清理缓存**
   ```bash
   rm -rf .next
   rm -rf node_modules/.cache
   ```

3. **重新安装依赖（如果需要）**
   ```bash
   npm install
   ```

4. **重新启动开发服务器**
   ```bash
   npm run dev
   ```

### 配置优化建议

#### 开发环境配置
- 避免在开发环境中使用复杂的 webpack 配置
- 将性能优化配置仅应用于生产环境
- 使用条件配置：`if (!dev && !isServer)`

#### 生产环境配置
- 在生产构建时应用代码分割和优化
- 使用 `npm run build` 测试生产配置
- 使用 `npm run analyze` 分析包大小

### 监控和调试

#### 检查服务器状态
```bash
# 检查进程是否运行
ps aux | grep "next dev"

# 检查端口是否响应
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000

# 检查详细输出
npm run dev -- --debug
```

#### 日志分析
- 查看控制台输出中的错误信息
- 注意 "Bail out to client-side rendering" 警告（通常是正常的）
- 检查编译错误和类型错误

### 常见错误和解决方案

#### 1. TypeScript 错误
```
Error: Type 'any' is not assignable...
```
**解决方案**: 修复类型定义，添加正确的类型注解

#### 2. ESLint 错误
```
Error: 'variable' is defined but never used
```
**解决方案**: 移除未使用的变量或添加 ESLint 忽略注释

#### 3. 动态导入警告
```
Bail out to client-side rendering: next/dynamic
```
**解决方案**: 这是正常的，表示组件将在客户端渲染

#### 4. 模块解析错误
```
Module not found: Can't resolve...
```
**解决方案**: 检查导入路径，确保文件存在

### 性能优化后的注意事项

1. **动态导入组件**
   - 确保所有动态导入的组件都有正确的类型定义
   - 提供适当的加载状态组件
   - 测试组件在不同网络条件下的加载表现

2. **脚本加载优化**
   - 验证第三方脚本是否正确加载
   - 测试用户交互触发的脚本加载
   - 确保错误处理机制正常工作

3. **代码分割**
   - 监控包大小变化
   - 确保关键路径不被过度分割
   - 测试首次加载性能

### 预防措施

1. **渐进式优化**
   - 一次只添加一个优化配置
   - 每次更改后测试开发服务器启动
   - 保留工作配置的备份

2. **环境分离**
   - 开发环境优先考虑启动速度
   - 生产环境优先考虑性能优化
   - 使用环境变量控制配置

3. **定期维护**
   - 定期清理缓存
   - 更新依赖包
   - 监控构建时间和包大小

如果问题仍然存在，请检查：
- Node.js 版本兼容性
- 依赖包版本冲突
- 系统资源使用情况
- 网络连接问题 